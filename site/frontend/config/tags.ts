export const ALLOWED_TAGS = [
  'глубокая глотка', 'сперма на лицо', 'сперма в рот', 'кремпай', 'сперма на грудь', 'сперма на попу', 'проглатывание', 'сквирт', 'золотой дождь', 'оральный секс', 'кунилингус', 'анальный секс', 'мастурбация', 'титджоб', 'догги-стайл', 'поза 69', 'эротический массаж', 'шлепки', 'секс-игрушки', 'вибратор', 'анальная пробка', 'страпон', 'фут-фетиш', 'огромный член', 'большая грудь', 'ошейник', 'грубый секс', 'жесткий анал', 'доминирование', 'подчинение', 'унижение', 'жесткое принуждение', 'связывание', 'кляп', 'порка', 'удушение', 'двойное проникновение', 'фистинг', 'групповой секс', 'мжм', 'жмж', 'свинг', 'гэнгбэнг', 'лесби', 'бисекс', 'милфа', 'пышка', 'студентка', 'девственница', 'госпожа', 'рабыня', 'хозяин', 'шлюха', 'развратная жена', 'папик', 'босс и подчиненная', 'секретарша', 'горничная', 'учительница', 'учитель и ученица', 'репетитор', 'соседка', 'подруга жены', 'жена друга', 'семейное табу', 'мачеха', 'отчим', 'сводная сестра', 'сводный брат', 'измена', 'вынужденная измена', 'куколд', 'тайная связь', 'первый раз', 'обучение сексу', 'секс за зачет', 'секс за деньги', 'оплата долга', 'съемки порно', 'кастинг', 'нежность', 'шантаж', 'месть', 'спор', 'афродизиак', 'пьяный секс', 'пробуждение', 'спящая', 'вуайеризм', 'публичный секс', 'секс в машине', 'секс в душе', 'в туалете', 'в офисе', 'на природе', 'баня', 'сауна', 'дача', 'отель', 'бассейн', 'курорт', 'фэнтези'
] as const;

const ALLOWED_TAGS_SET = new Set<string>(ALLOWED_TAGS);

export function normalizeTag(tag: string): string {
  return tag.trim().toLowerCase();
}

export function toDisplayTag(tag: string): string {
  if (!tag) return tag;
  return `${tag.charAt(0).toUpperCase()}${tag.slice(1)}`;
}

export function filterAllowedTags(tags: string[] | undefined): string[] {
  if (!tags || tags.length === 0) return [];
  const result: string[] = [];
  const seen = new Set<string>();
  for (const tag of tags) {
    const clean = normalizeTag(tag);
    if (!clean || !ALLOWED_TAGS_SET.has(clean) || seen.has(clean)) continue;
    seen.add(clean);
    result.push(toDisplayTag(clean));
  }
  return result;
}
